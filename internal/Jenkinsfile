pipeline {
    agent any

    stages {
        stage('Trigger downstream') {
            steps {
                build job: 'matter-github-mirror-artifactory', parameters:[string(name: 'branchName', env.BRANCH_NAME), string(name:'buildNumber', value: env.BUILD_NUMBER)]
            }
        }
    }
}