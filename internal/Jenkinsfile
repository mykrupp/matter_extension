pipeline {
    agent any
    // Trigger job 15
    stages {
        stage('Trigger downstream') {
            steps {
                build job: 'Matter GitHub Jenkins/sqa_test_branch', parameters:[string(name: 'branchName', value: env.BRANCH_NAME), string(name:'buildNumber', value: env.BUILD_NUMBER)]
            }
        }
    }
}